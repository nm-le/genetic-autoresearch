# exp0026 — halve effective batch again (TOTAL_BATCH_SIZE=2**17, DEVICE=64)

- parent1: 40e0861 (elite/2 = exp0022, lr=0.045, no warmup, batch=2**18,
  device=128)
- parent2: -
- operator: mutate
- what changed: `TOTAL_BATCH_SIZE = 2**18 → 2**17`, `DEVICE_BATCH_SIZE =
  128 → 64`. Coupled change to keep `grad_accum_steps=1` legal — both halve
  in lockstep, implementing one logical idea: "halve effective batch again".

## Parent metrics
- elite/2: val_bpb=0.989286 (batch=2**18)

## Child metrics
- val_bpb: 0.994657
- peak_vram_mb: 22701.2 (~22.2 GB — half of parent)
- num_params_M: 50.3
- num_steps: 3476 (~1.9× more than parent)
- training_seconds: 300.1

## Deltas
- vs parent1 (elite/2): +0.005371 (regression)
- vs current best: +0.006621
- VRAM saving: -22 GB (huge headroom freed)

## Decision
Going further on the batch-halve axis hurts. Optimum is at batch=2**18
(elite/0). Gradient noise dominates the gain from extra updates at this
batch size. Don't promote.

## Revisit
- The freed VRAM (44GB → 22GB) is a strong hint: scaling axes that need
  memory (DEPTH=10, larger ASPECT_RATIO, all-long window pattern, larger
  vocab embeddings) might fit comfortably. Especially DEPTH=10 with
  batch=2**18 might compose well — bigger model + 2× more steps of the
  bigger model.
- Skip further halving; we've found the bottom of this axis.

## Closest prior experiment
exp0022 (batch=2**18, val=0.989286). Δ = +0.005371.

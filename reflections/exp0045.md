# exp0045 — Muon ns_steps 5 → 4

- parent1: d07e930 (elite/0, depth=10, lr=0.03, warmup, batch=2**18)
- parent2: -
- operator: mutate
- what changed: in `setup_optimizer`, Muon `ns_steps=5` → `4`. Saves
  one polar-express orthogonalization pass per Muon step.

## Parent metrics
- elite/0 (best): val_bpb=0.985189

## Child metrics
- val_bpb: 0.986822
- peak_vram_mb: 67804.2
- num_params_M: 85.9
- num_steps: 1121
- training_seconds: 300.2

## Deltas
- vs parent1: +0.001633 (significant regression)
- step count: +1 (no measurable speedup — torch.compile probably folds
  the polar-express loop and the savings show as kernel-level, not step
  count)

## Decision
Less orthogonalization clearly hurts. The 5th polar-express coefficient
contributes meaningfully even though absolute coefficients are small.
Worse, fewer steps weren't gained. Don't promote.

## Revisit
- Don't reduce ns_steps. Could try 6 once (more accurate orthogonalization)
  but expectation is that current 5 is well-tuned.

## Closest prior experiment
exp0042 (ns_steps=5, identical otherwise, val=0.985189). Δ = +0.001633.

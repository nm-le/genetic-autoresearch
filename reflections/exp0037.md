# exp0037 — DEPTH 8 → 9 (mutate elite/0) — **HUGE NEW BEST**

- parent1: `dbbdc5a` (elite/0: depth=8 + small-batch + best other knobs, val=0.986408)
- parent2: `-`
- operator: mutate
- what changed: `DEPTH = 9` (was 8). Knock-on: model_dim 512→640, n_head 4→5, params 50.3M→80.9M.
- child metrics:
  - val_bpb: **0.983665**
  - num_steps: 2374
  - vram: 31.2 GB
  - params: 80.9M
- deltas: vs elite/0 (best): **−0.002743 (NEW GLOBAL BEST)**.
- decision summary: DEPTH=9 was bad at large batch (exp0002: 1.018) but now WORKS at the small-batch regime. The critical change: at small batch we get more updates per second, so the bigger model has enough optimization steps to make use of its capacity. Promote to elite/0.
- revisit: HIGH PRIORITY:
  1. DEPTH=10 (continue scaling).
  2. ASPECT_RATIO=80 from this base (wider model).
  3. Re-tune MATRIX_LR for bigger model (might prefer slightly lower LR).
  4. WD sweep at this DEPTH=9 regime.
- closest prior: exp0035 (parent), exp0002 (DEPTH=9 at large batch — bad).

# exp0016

parent1: elite/0
parent2: -
mutation_kind: mutate
closest_prior: exp0014, exp0010

change_summary:

- UNEMBEDDING_LR: 0.012 -> 0.016 (re-test under new TOTAL_BATCH_SIZE=2**18 regime)

parent_metrics:

- val_bpb: 0.989614

child_metrics:

- val_bpb: 0.987732
- num_steps: 1811

deltas:

- delta_val_bpb: -0.001882

decision:

- status: elite, replace_slot=elite/0

judgment:

- big finding: ULR=0.016 was an OVERSHOOT under TOTAL=2**19 (914 steps, exp0010=0.997275) but is a WIN under TOTAL=2**18 (1811 steps).
- with twice as many gradient steps, the model can absorb a more aggressive lm_head LR.
- general pattern: prior knob optima found under the old step regime should be re-checked.

revisit_later:

- yes — try ULR=0.020 next
- and re-sweep MATRIX_LR (currently 0.04, may want higher under new regime)

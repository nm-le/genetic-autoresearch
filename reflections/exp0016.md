# exp0016 — x0_lambdas init 0.1 → 0.05

- parent1: c50e49f (elite/3, lr=0.045, warmup=0.05)
- parent2: -
- operator: mutate
- what changed: `init_weights`: `self.x0_lambdas.fill_(0.1)` → `0.05`
  (less initial mixing of input embedding into each layer's residual).

## Parent metrics
- elite/3: val_bpb=0.995707

## Child metrics
- val_bpb: 0.996289
- peak_vram_mb: 45060.2
- num_params_M: 50.3
- training_seconds: 300.2

## Deltas
- vs parent1: +0.000582 (regression)

## Decision
Smaller x0 init slows the model's path to mixing input information back into
each layer. With Adam learning these per-layer scalars, smaller init costs us
training steps to recover. Don't promote.

## Revisit
Could try LARGER x0 init (0.15, 0.2) — opposite direction.

## Closest prior experiment
exp0004 (x0_init=0.1). Δ = +0.000582.

# exp0047 — rotary base 10000 → 50000

- parent1: 49a6e5c (elite/2 = exp0044, depth=10, lr=0.03375, warmup,
  embedding_lr=0.4, batch=2**18)
- parent2: -
- operator: mutate
- what changed: in `_precompute_rotary_embeddings`, default `base=10000`
  → `50000`. Larger rotary base = lower frequency rotations across
  positions = effectively longer context modelling.

## Parent metrics
- elite/2: val_bpb=0.985558
- elite/0 (best): val_bpb=0.985189

## Child metrics
- val_bpb: 0.985170
- peak_vram_mb: 67804.2
- num_params_M: 85.9
- num_steps: 1121
- training_seconds: 300.1

## Deltas
- vs parent1: -0.000388
- vs current best: -0.000019 (within noise floor of ±0.0002)

## Decision
Within noise of best. Cannot conclude rotary_base=50000 is a real win, but
combined with embedding=0.4 + lr=0.03375 base, the rotary tweak likely
contributed something positive. Promote — replaces elite/2 as improved
parent slot.

## Revisit
- Try rotary base=50000 on top of clean best config (lr=0.03 +
  embedding=0.6 + warmup + depth=10) to isolate the rotary effect.
- Try rotary base=100000 (extrapolation).

## Closest prior experiment
exp0042 (best, base=10000, val=0.985189). Δ = -0.000019 (essentially tied).

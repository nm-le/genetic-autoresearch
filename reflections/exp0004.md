# exp0004 — add LR warmup on top of elite/2

- parent1: 0c70db6 (elite/2, matrix_lr=0.045, no warmup)
- parent2: -
- operator: mutate
- what changed: `WARMUP_RATIO = 0.0` → `0.05` (5% of budget for LR warmup)

## Parent metrics
- elite/2: val_bpb=0.995706

## Child metrics
- val_bpb: 0.995707
- peak_vram_mb: 45060.2 (~44.0 GB)
- num_params_M: 50.3
- training_seconds: 300.2

## Deltas
- vs parent1: +0.000001 (essentially identical)
- vs best (elite/1, 0.995664): +0.000043

## Decision
Warmup at 5% with this Muon setup is a no-op. Probably because Muon's
orthogonalization step already absorbs early-step instability, and the LR
schedule doesn't need to ramp. Frontier still has slot 3 free → promotes
as diversity.

## Revisit
Skip warmup variants going forward unless we find evidence the early steps
are unstable (e.g. with much higher LRs). Don't tune in tiny increments.

## Closest prior experiment
exp0003 (matrix_lr=0.045, warmup=0). Δval_bpb = +0.000001.

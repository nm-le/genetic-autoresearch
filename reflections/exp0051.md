# exp0051 — remove logit softcap (simplification attempt)

- parent1: af87940 (elite/2 = exp0047, depth=10, lr=0.03375, embedding=0.4,
  warmup, base=50000)
- parent2: -
- operator: mutate
- what changed: removed lines `softcap = 15` and `logits = softcap *
  torch.tanh(logits / softcap)`. Drop logit bounding entirely.

## Parent metrics
- elite/2: val_bpb=0.985170

## Child metrics
- val_bpb: 0.991506
- peak_vram_mb: 67806.2
- num_params_M: 85.9
- num_steps: 1130 (slightly more — softcap added a tiny bit of compute)
- training_seconds: 300.1

## Deltas
- vs parent1: +0.006336 (severe regression)
- vs current best: +0.006317

## Decision
Softcap is load-bearing. Without it, logits drift unbounded and BPB
explodes. Confirms the tanh softcap is a real stabilizer at this scale.
Don't promote.

## Revisit
Don't remove softcap. Could try softcap=30 (less aggressive) but expectation
is similar to softcap=15.

## Closest prior experiment
exp0047 (with softcap, val=0.985170). Δ = +0.006336.

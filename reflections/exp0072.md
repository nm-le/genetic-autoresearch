# exp0072

parent1: elite/0
parent2: -
mutation_kind: mutate

change_summary:

- short_window: long_window // 2 (1024) -> long_window // 4 (512)

parent_metrics:

- val_bpb: 0.984035

child_metrics:

- val_bpb: 0.982218
- num_steps: 1547

deltas:

- delta_val_bpb: -0.001817 (real new best!)

decision:

- status: elite, replace_slot=elite/0

judgment:

- big finding: smaller short attention window improves quality. Likely because doc-packed sequences put unrelated documents within 1024-token range, and the SHORT layers benefit from being even more local.
- this is a surprising win — the prior all-L test (exp0023) was about-tied. Smaller S helps even more than going all-L.
- worth_it: yes — meaningful gain, no extra cost (slightly faster, more steps).

revisit_later:

- yes — try short_window=256 next (1/8) and check if trend continues

# exp0082

parent1: elite/0
parent2: -
mutation_kind: mutate
closest_prior: exp0074, exp0079

change_summary:

- short_window: 256 -> 128 with DEPTH=12 (was DEPTH=10 when 128 was tested before)

parent_metrics:

- val_bpb: 0.980832

child_metrics:

- val_bpb: 0.980502
- num_steps: 1369

deltas:

- delta_val_bpb: -0.000330 (small new best)

decision:

- status: elite, replace_slot=elite/0

judgment:

- the depth-window interaction continues: more layers + smaller per-layer window keeps improving.
- prior test of 128 at DEPTH=10 (exp0074) was slightly worse than 256, but at DEPTH=12 the smaller window is now best.

revisit_later:

- yes — try short_window=64 next

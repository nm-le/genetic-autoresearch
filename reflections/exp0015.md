# exp0015

parent1: elite/1 (per crossover rule)
parent2: elite/0
mutation_kind: crossover
closest_prior: exp0014

change_summary:

- TOTAL_BATCH_SIZE: 2**18 -> 2**17 (131K)
- DEVICE_BATCH_SIZE: 128 -> 64 (forced, to keep grad_accum=1)

parent_metrics:

- elite/0: val_bpb=0.989614, num_steps=1807

child_metrics:

- val_bpb: 0.995455
- num_steps: 3291
- mfu_percent: 34.62
- peak_vram_mb: 22701 (half of elite/0!)

deltas:

- delta_val_bpb: +0.005841 (worse)

decision:

- status: discard

judgment:

- 2**18 is the optimal batch size. Going smaller introduces too much gradient noise even with Muon orthogonalization, and the doubled step count cannot compensate.
- side note: memory halved with DB=64 — useful if we ever want a bigger model.

revisit_later:

- not on this batch sweep axis
- but DB=64 unlocks ~22GB headroom for architecture experiments (deeper/wider models)

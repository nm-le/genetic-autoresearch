# exp0008

parent1: elite/1 (baseline)
parent2: elite/0 (exp0007: MATRIX_LR=0.06, UNEMBEDDING_LR=0.008)
mutation_kind: crossover
closest_prior: exp0007

change_summary:

- baseline + UNEMBEDDING_LR=0.008 (transplant only the unembedding LR from elite/0; keep MATRIX_LR=0.04)

parent_metrics:

- elite/1 (baseline): val_bpb=0.997811
- elite/0 (combo): val_bpb=0.995899

child_metrics:

- val_bpb: 0.995873
- num_steps: 914

deltas:

- vs baseline: -0.001938
- vs combo (elite/0): -0.000026 (essentially tied)

decision:

- status: elite
- replace_slot: elite/0 (replaces exp0007 because it is simpler — only one change from baseline — and equal performance)

judgment:

- Big finding: UNEMBEDDING_LR=0.008 alone captures essentially all of exp0007's gain. The MATRIX_LR=0.06 contribution from exp0004 (-0.001036 vs baseline) is mostly redundant once UNEMBEDDING_LR is bumped.
- This means earlier MATRIX_LR sweep was likely just compensating for an undertrained lm_head.
- worth_it: yes — strictly simpler than the combo

revisit_later:

- yes — try UNEMBEDDING_LR=0.012 to find ceiling
- also revisit MATRIX_LR sweep _from this new base_ (the LR optimum may differ once lm_head is well-trained)

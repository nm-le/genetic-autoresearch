# exp0008 — ADAM_BETAS (0.8, 0.95) → (0.9, 0.95)

- parent1: c256980 (elite/0, current best, matrix_lr=0.0475, betas=(0.8,0.95))
- parent2: -
- operator: mutate
- what changed: `ADAM_BETAS = (0.8, 0.95)` → `(0.9, 0.95)` (more standard beta1)

## Parent metrics
- elite/0: val_bpb=0.995644

## Child metrics
- val_bpb: 0.996416
- peak_vram_mb: 45060.2
- num_params_M: 50.3
- training_seconds: 300.3

## Deltas
- vs parent1: +0.000772 (significant regression)
- vs baseline: +0.000001 (effectively baseline)

## Decision
beta1=0.9 hurts at short training horizons. The lower beta1=0.8 in the
original code is intentional — Adam's exp_avg builds faster, which matters
when you only get ~950 steps. Discard.

## Revisit
Don't try beta1=0.9. Possibly try beta1=0.7 (even faster averaging) on a
future run, but only with conviction.

## Closest prior experiment
exp0001 baseline (val_bpb=0.996415). Effectively reproduced baseline despite
having matrix_lr=0.0475 — suggesting the gain from matrix_lr is fully erased
by the wrong beta1.

# exp0019 — WINDOW_PATTERN SSSL → SLSL

- parent1: 0c70db6 (elite/2, lr=0.045, SSSL)
- parent2: -
- operator: mutate
- what changed: `WINDOW_PATTERN = "SSSL"` → `"SLSL"`. Half the layers now
  use full attention instead of 1/4. Compute per step grows.

## Parent metrics
- elite/2: val_bpb=0.995706

## Child metrics
- val_bpb: 0.997496
- peak_vram_mb: 45060.2
- num_params_M: 50.3
- num_steps: 924 (vs ~953 for SSSL)
- training_seconds: 300.2

## Deltas
- vs parent1: +0.001790 (significant regression)
- step count: -3% (~29 fewer steps)

## Decision
SLSL is materially worse. Either (a) long context doesn't help much with
2048 sequence length and short training, or (b) the slower step time burns
budget without gain. Don't promote.

## Revisit
Skip alternative window patterns (LLLL would be even slower). The default
SSSL is well-tuned.

## Closest prior experiment
exp0003 (SSSL). Δ = +0.001790.

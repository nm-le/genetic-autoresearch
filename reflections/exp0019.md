# exp0019 — WEIGHT_DECAY 0.15 → 0.10 (mutate elite/0)

- parent1: `c57cda9` (elite/0: depth=8 + MATRIX_LR=0.06 + EMBEDDING_LR=0.5 + WEIGHT_DECAY=0.15, val=0.995722)
- parent2: `-`
- operator: mutate
- what changed: `WEIGHT_DECAY = 0.10` (was 0.15).
- child metrics: val_bpb=**0.995604**; vram=44.0 GB, params=50.3M, num_steps=927
- deltas:
  - vs parent: **−0.000118 (BETTER, new effective best)**
  - vs baseline (exp0001): −0.001294
- decision summary: WEIGHT_DECAY=0.10 keeps improving on the WEIGHT_DECAY sweep direction. Promotes via parent-slot improvement (just under the 0.000150 strict-best margin so not "new global best" formally, but still better than prior best). Promote to elite/0.
- revisit: continue the sweep — WEIGHT_DECAY=0.05 next, then 0.0 if 0.05 still helps.
- closest prior: exp0017 (parent), the WEIGHT_DECAY sweep predecessor.

## WEIGHT_DECAY sweep at depth=8 + MATRIX_LR=0.06 + EMBEDDING_LR=0.5:
- 0.20 → 0.996634 (exp0014)
- 0.15 → 0.995722 (exp0017)
- **0.10 → 0.995604 (exp0019)**
- 0.05, 0.0 → todo

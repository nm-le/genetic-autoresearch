# exp0076 — crossover: x0=0.2 + ve_gate=64 onto clean base

- parent1: af26d3e (elite/3 = exp0064, lr=0.03375, embedding=0.6,
  warmup, base=10000, FINAL_LR_FRAC=0.05, x0=0.1, ve_gate=32)
- parent2: 0f66a14 (elite/2 best, x0=0.2 + ve_gate=64 + embedding=0.4
  + base=50000)
- operator: crossover
- what changed: transplanted parent2's x0=0.2 AND ve_gate=64 onto
  parent1; left embedding=0.6 and base=10000 unchanged.

## Parent metrics
- elite/3: val_bpb=0.984379
- elite/2 (best): val_bpb=0.983089

## Child metrics
- val_bpb: 0.985658
- peak_vram_mb: 67805.3
- num_params_M: 85.9
- num_steps: 1124
- training_seconds: 300.2

## Deltas
- vs parent1: +0.001279 (clear regression)
- vs current best: +0.002569

## Decision
**Confirms: the best (0.983089) requires the FULL stack** —
embedding=0.4 + base=50000 + x0=0.2 + ve_gate=64. Removing either
embedding=0.4 or base=50000 destroys the win.

The x0=0.2 + ve_gate=64 architectural pair without the LR/embedding
stack is *worse* than baseline (x0=0.1, ve_gate=32). This is a strong
multi-axis interaction.

Don't promote.

## Revisit (high priority)
- Test x0=0.2 + ve_gate=64 + embedding=0.4 (no base=50000): isolates
  the role of base=50000.
- Test x0=0.2 + ve_gate=64 + base=50000 (no embedding=0.4): isolates
  the role of embedding=0.4.

## Closest prior experiment
exp0064 (clean parent, x0=0.1, ve_gate=32). Δ = +0.001279.

# exp0014

parent1: elite/0
parent2: -
mutation_kind: mutate
closest_prior: exp0009

change_summary:

- TOTAL_BATCH_SIZE: 2**19 (524K) -> 2**18 (262K)
- grad_accum_steps: 2 -> 1 (DEVICE_BATCH_SIZE=128 unchanged)

parent_metrics:

- val_bpb: 0.995600
- num_steps: 914
- mfu_percent: 38.13

child_metrics:

- val_bpb: 0.989614
- num_steps: 1807
- mfu_percent: 37.92

deltas:

- delta_val_bpb: -0.005986 (huge improvement)
- delta_steps: +893 (~2x)
- delta_memory_gb: -0.1

decision:

- status: elite, replace_slot=elite/0

judgment:

- biggest single-knob win in this run by far. The baseline TOTAL_BATCH_SIZE=2^19 was significantly oversized for this 5-min budget — the model is undertrained and benefits from twice as many optimizer steps with noisier gradients (Muon orthogonalization smooths the noise). 
- worth_it: absolutely yes — same memory, same params, dramatically better val_bpb.

revisit_later:

- yes — try TOTAL_BATCH_SIZE=2**17 (with DEVICE_BATCH_SIZE=64 to keep grad_accum=1) to see if the trend continues
- many earlier knob optima may shift now that step count doubled
- consider re-sweeping MATRIX_LR around 0.04 with new step regime (LR may need slight adjustment)

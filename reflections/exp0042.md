# exp0042 — crossover: literal lr=0.03 transplant (config-equiv to exp0034)

- parent1: 3d0e472 (elite/1 = exp0039, depth=10, lr=0.035, warmup,
  batch=2**18)
- parent2: f58a279 (elite/0 = exp0034, depth=10, lr=0.03, warmup,
  batch=2**18)
- operator: crossover
- what changed: literal transplant of parent2's lr=0.03 onto parent1
  (which differed from parent2 only in matrix_lr). Code is identical to
  exp0034.

## Parent metrics
- elite/1: val_bpb=0.985562
- elite/0 (best): val_bpb=0.985399

## Child metrics
- val_bpb: 0.985189  ← *technically* new global best
- peak_vram_mb: 67804.2
- num_params_M: 85.9
- num_steps: 1120
- training_seconds: 300.0

## Deltas
- vs parent1: -0.000373
- vs parent2 / exp0034 (identical config): -0.000210

## Decision
Same code as exp0034 → the 0.000210 delta is pure GPU non-determinism
(torch.compile / CUDA reductions ordering). Important calibration: the
**run-to-run noise floor on this setup is roughly ±0.0002**. Many of the
fine-grained "wins" between 0.985189 and 0.985768 are within noise.
Promote per the controller rule (clears margin), but treat with caution.

## Revisit
- All single-knob LR/warmup/embedding deltas of <0.0003 are at or below
  noise. Trust only effects ≥ ~0.0005.
- Worth running exp0034 config 3-5x to estimate noise distribution
  precisely. Skip for now — single re-run already enough to flag the
  caveat.

## Closest prior experiment
exp0034 (identical code). Δ = -0.000210. Pure noise.

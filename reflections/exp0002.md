# exp0002 — DEPTH 8 → 9

- parent1: `fcc3407` (elite/0, baseline)
- parent2: `-`
- operator: mutate
- what changed: `DEPTH = 9` (was 8). Knock-on: `model_dim` grew 512 → 640 (rounded up to multiple of HEAD_DIM=128), `num_heads` 4 → 5. Params 50.3M → 80.9M.
- parent metrics:
  - val_bpb: 0.996898
  - peak_vram_mb: 45060.2
  - num_steps: 926
- child metrics:
  - val_bpb: 1.017796
  - peak_vram_mb: 61917.0 (~60.5 GB)
  - num_params_M: 80.9
  - num_steps: 616
  - mfu_percent: 41.87
- deltas:
  - val_bpb: **+0.020898 (WORSE)**
  - vram: +16.9 GB
  - params: +30.6 M (+61%)
  - steps: -310 (-33%)
  - mfu: +3.20 (better hardware utilization but the bigger model didn't pay off)
- decision summary: bigger model trained on 33% fewer steps; the per-step learning gain didn't compensate. Promoted to `elite/1` because frontier was not full (population=4), but it serves as a "too big" data point, not a real win.
- revisit: do **not** revisit DEPTH=9 alone. Could revisit only as part of a larger configuration shift (e.g. with smaller batch size, or tuned LRs that suit the bigger model). In this time-budgeted regime, the baseline depth=8 is preferred.
- closest prior: exp0001 (baseline, the parent).

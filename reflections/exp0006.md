# exp0006 — crossover elite/0 + elite/3 → MATRIX_LR=0.05 on baseline

- parent1: `fcc3407` (elite/0, baseline depth=8, val=0.996898)
- parent2: `64f83ef` (elite/3, depth=9+warmdown=0.3+MATRIX_LR=0.05, val=1.012976)
- operator: crossover
- what changed: from baseline (depth=8, warmdown=0.5), transplanted ONE coherent idea from elite/3 — `MATRIX_LR = 0.05` (was 0.04). Did NOT pull in depth=9 or warmdown=0.3.
- parent metrics:
  - elite/0: val_bpb=0.996898, num_steps=926
  - elite/3: val_bpb=1.012976
- child metrics:
  - val_bpb: 0.996469
  - peak_vram_mb: 45060.2 (~44.0 GB)
  - num_params_M: 50.3
  - num_steps: 927
  - mfu_percent: 38.69
- deltas:
  - vs baseline elite/0: **−0.000429 (BETTER — new global best)**
  - vs elite/3 (parent2): −0.016507
- decision summary: a 25% bump in MATRIX_LR is a small but real improvement on baseline. First experiment to clear the baseline. Promote into elite/0 as new best. The crossover protocol paid off here — taking one knob change (MATRIX_LR=0.05) from a much-worse big-model elite and dropping it onto the baseline turned out to be a genuinely productive recombination.
- revisit: yes — sweep MATRIX_LR further (e.g. 0.045, 0.055, 0.06) to find the local optimum at depth=8. Also: try MATRIX_LR=0.05 combined with other small tweaks from baseline (this builds the "improved baseline" that future search should branch off).
- closest prior: exp0001 (baseline) for arch, exp0004 for the LR=0.05 magnitude tested at depth=9.

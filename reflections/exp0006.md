# exp0006 — shorten WARMDOWN_RATIO 0.5 → 0.3

- parent1: c50e49f (elite/3, lr=0.045, warmup=0.05, warmdown=0.5)
- parent2: -
- operator: mutate
- what changed: `WARMDOWN_RATIO = 0.5 → 0.3`. More steps at peak LR.

## Parent metrics
- elite/3: val_bpb=0.995707

## Child metrics
- val_bpb: 0.997789
- peak_vram_mb: 45060.2
- num_params_M: 50.3
- training_seconds: 300.0

## Deltas
- vs parent1: +0.002082 (significant regression)
- vs baseline: +0.001374 (worse than baseline)

## Decision
Clear regression. The model needs the longer cooldown phase to converge —
intuition: with this LR schedule, fewer cooldown steps = parameters not fully
settled by eval time. Discard.

## Revisit
Don't shorten warmdown. If anything, try LONGER warmdown (0.6, 0.7) on a
future run. Or pair this with FINAL_LR_FRAC > 0.

## Closest prior experiment
exp0004 (warmdown=0.5). Δ = +0.002082.

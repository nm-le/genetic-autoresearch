# exp0053 — DEPTH 10→12 + ASPECT_RATIO 64→48 (CRASH: OOM)

- parent1: d07e930 (elite/0, depth=10, lr=0.03, warmup, batch=2**18)
- parent2: -
- operator: mutate
- what changed: coupled changes — `DEPTH = 10 → 12` and
  `ASPECT_RATIO = 64 → 48`. Aim: deeper model at same model_dim=640
  (12 × 48 = 576 → rounds to 640).

## Outcome
Crashed with OOM at the lm_head logits buffer (262144 × 8192). 100.9M
params, but activations from 12 layers × batch=128 × seq=2048 push past
80 GB.

## Stats before crash
- params: 100.9M
- model_dim: 640 (5 heads × 128)
- step 0 logged loss only

## Decision
DEPTH=12 needs DEVICE_BATCH_SIZE reduction to fit. That would be a
3-axis change (batch + grad_accum coordination), too entangled for a
single mutation. Skip depth=12 within this batch=2**18 + DEVICE=128
regime.

## Revisit
- Try DEPTH=11 (one less layer than this attempt) at same ASPECT=48
  → model_dim still 640. May fit in ~75 GB.
- Or lower DEVICE_BATCH_SIZE to 64 first, then explore depth=12.

## Closest prior experiment
exp0040 (also OOM with bigger model). Same wall.

# exp0039 — DEPTH 10 → 11 (mutate elite/0)

- parent1: `df39ee2` (elite/0: DEPTH=10 + small-batch + best, val=0.983138)
- parent2: `-`
- operator: mutate
- what changed: `DEPTH = 11` (was 10). Knock-on: model_dim 640→768 (since 11*64=704, rounded up to 768=6*128). n_head 5→6. params 85.9M→128.2M.
- child metrics: val_bpb=0.984471; vram=44.8 GB, params=128.2M, num_steps=1554
- deltas: vs elite/0 (best): **+0.001333 (WORSE)**.
- decision summary: DEPTH=11 jumps params by 49% (extra layer + width bump). Steps drop from 2169 → 1554 (28% fewer). The extra capacity doesn't compensate for the lost step count. Replaces elite/3 (frontier diversity).
- revisit: try DEPTH=11 with ASPECT_RATIO=64 forcing model_dim back to 640? But that's a non-standard config: 11*64=704 doesn't divide 128 well. Skip.
  Better: try wider model at DEPTH=10 (ASPECT_RATIO=72 → model_dim 720 → 768).
  Or: try DEVICE_BATCH=32 to free more VRAM and double steps.
- closest prior: exp0038 (parent at DEPTH=10).

## DEPTH sweep at small-batch base:
- 8 → 0.986408 (was best)
- 9 → 0.983665 (better)
- **10 → 0.983138 (peak so far)**
- 11 → 0.984471 (worse, fewer steps)

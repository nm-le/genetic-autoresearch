# exp0030 — TOTAL_BATCH_SIZE 2**19 → 2**18 (mutate elite/3) — **MAJOR NEW BEST**

- parent1: `99be0ce` (elite/3: depth=8 + MATRIX_LR=0.065 + EMB=0.5 + WD=0.10, val=0.996065)
- parent2: `-`
- operator: mutate
- what changed: `TOTAL_BATCH_SIZE = 2**18` (was 2**19). With DEVICE_BATCH_SIZE=128 and MAX_SEQ_LEN=2048, this drops grad_accum_steps from 2 to 1 — every fwd/bwd is now a full optimizer step.
- child metrics:
  - val_bpb: **0.989060**
  - num_steps: **1841** (was ~928)
  - vram: 44908.2 MB
  - params: 50.3M
- deltas:
  - vs elite/3 parent: **−0.007005**
  - vs elite/0 prior best: **−0.006544 (HUGE NEW GLOBAL BEST)**
  - vs baseline (exp0001=0.996898): −0.007838
- decision summary: Doubling the optimizer step count (by halving the per-step batch) is the single biggest gain in the run. The model was clearly under-optimizing before — more updates with the same per-update gradient quality wins by a wide margin in the 5-min budget. New global best, promote to elite/0.
- revisit: VERY HIGH PRIORITY — this is a new "knob frontier":
  1. Try TOTAL_BATCH=2**18 onto elite/0 base (MATRIX_LR=0.06 instead of 0.065) — might be even better.
  2. Try TOTAL_BATCH=2**17 (even smaller, 4x more updates).
  3. With smaller batch, LR landscape may shift — re-explore MATRIX_LR, EMB_LR.
- closest prior: none — this opens a new region of the search space.

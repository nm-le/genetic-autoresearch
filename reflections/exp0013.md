# exp0013

parent1: elite/0
parent2: -
mutation_kind: mutate
closest_prior: exp0009

change_summary:

- DEPTH: 8 -> 10 (model_dim 512 -> 640 via ASPECT_RATIO scaling, n_head 4 -> 5)

parent_metrics:

- val_bpb: 0.995600
- memory_gb: 44.0
- params_m: 50.3
- num_steps: 914

child_metrics:

- val_bpb: 1.003005
- memory_gb: 66.5
- params_m: 85.9
- num_steps: 559

deltas:

- delta_val_bpb: +0.007405 (much worse)
- delta_memory: +22.5 GB
- delta_params: +35.6M (70% increase)
- delta_steps: -355

decision:

- status: discard

judgment:

- bigger model is severely undertrained at this 5-min budget. Param doubling needs proportional token doubling, not feasible.
- the 50M baseline size is in the sweet spot for this budget.

revisit_later:

- could try DEPTH=10 with ASPECT_RATIO=51 (constant model_dim=512, only +2 layers, ~58M params) for a cleaner depth-only test
- or try DEPTH=6 (smaller, faster) for "lean" diversity slot

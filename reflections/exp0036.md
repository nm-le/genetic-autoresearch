# exp0036 — TOTAL_BATCH=2**16 + DEVICE_BATCH=32 (mutate elite/0)

- parent1: `dbbdc5a` (elite/0: TOTAL_BATCH=2**17 + WARMDOWN=0.6 + best, val=0.986408)
- parent2: `-`
- operator: mutate
- what changed: `TOTAL_BATCH_SIZE = 2**16` (was 2**17), `DEVICE_BATCH_SIZE = 32` (was 64).
- child metrics: val_bpb=0.996460; vram=11.6 GB, params=50.3M, num_steps=6184
- deltas: vs parent: **+0.010052 (much WORSE)**.
- decision summary: smaller-batch sweep REVERSED at TOTAL_BATCH=2**16. The peak is around 2**17. Below that, gradient noise + too-small batches break the optimization (or the LR settings tuned for larger batch don't transfer). Not promoted.
- revisit: stop reducing batch. The peak is at TOTAL_BATCH=2**17 + DEVICE_BATCH=64. Move to other axes — bigger model (using freed VRAM), or LR re-sweep at the new batch.

## TOTAL_BATCH sweep at depth=8 + best other knobs:
- 2**19 → ~0.996 (old best)
- 2**18 → 0.987464 (huge gain)
- 2**17 → 0.987300 / 0.986408 (with warmdown=0.6 — best so far)
- 2**16 → 0.996460 (this run, regression)

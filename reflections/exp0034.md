# exp0034 — DEPTH 8 → 10 (HUGE WIN)

- parent1: bc9a56e (elite/3 = exp0031, depth=8, lr=0.03 + warmup +
  batch=2**18)
- parent2: -
- operator: mutate
- what changed: `DEPTH = 8 → 10`. Architecture grows: model_dim 512 → 640
  (5 heads × 128), params 50.3M → 85.9M.

## Parent metrics
- elite/3: val_bpb=0.987913, params=50.3M

## Child metrics
- val_bpb: 0.985399  ← **new global best (HUGE)**
- peak_vram_mb: 67804.2 (~66.2 GB)
- num_params_M: 85.9
- num_steps: 1120
- training_seconds: 300.2

## Deltas
- vs parent1 (depth=8): -0.002514
- vs prior best (exp0032, depth=10 lr=0.04 no-warmup): -0.001858
- vs depth=8 best (exp0030, lr=0.035): -0.002096

## Decision
Massive jump. The depth-10 lr curve at batch=2**18 + warmup so far:
- lr=0.03 + warmup, depth=10: 0.985399  ← new minimum
- lr=0.04, no warmup, depth=10: 0.987257
- lr=0.05, warmup,   depth=10: 0.987406

Lower lr + warmup wins decisively at depth=10. The deeper model benefits
from gentler learning. Promote → new elite/0.

## Revisit
- Try matrix_lr=0.025 + warmup + depth=10 (push lower).
- Try matrix_lr=0.02 + warmup + depth=10.
- Try DEPTH=12 if VRAM allows (tight at ~66 GB now; 12 might OOM).
- Try DEPTH=10 + lr=0.03 + no_warmup (does warmup matter at depth=10?).

## Closest prior experiment
exp0032 (depth=10, lr=0.04, no warmup, val=0.987257). Δ = -0.001858.

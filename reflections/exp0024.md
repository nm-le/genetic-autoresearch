# exp0024

parent1: elite/1 (per crossover rule)
parent2: elite/0
mutation_kind: crossover
closest_prior: exp0013

change_summary:

- elite/0 base + DEPTH 8 -> 10 with ASPECT_RATIO 64 -> 51 (keeps model_dim=512)
- params: 50.3M -> 60.8M (+21%)
- memory: 43.9 -> 53.7 GB (+22%)
- num_steps: 1808 -> 1493 (-17%)

parent_metrics:

- elite/0: val_bpb=0.987732

child_metrics:

- val_bpb: 0.985635

deltas:

- delta_val_bpb: -0.002097 (better!)

decision:

- status: elite, replace_slot=elite/0

judgment:

- big finding: depth-only scaling (constant model_dim) wins. Adding 2 more layers improves val_bpb despite ~17% fewer steps.
- the previous DEPTH=10 attempt (exp0013) scaled BOTH depth and width via default ASPECT_RATIO, which made the model 70% bigger and undertrained. With width pinned, the depth scaling is a net win.

revisit_later:

- yes — try DEPTH=12 with AR=42 (model_dim=512) to see if the trend continues
- also consider depth=10 with a smaller width (e.g., model_dim=384) for lean experiment

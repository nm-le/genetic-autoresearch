# exp0101

parent1: elite/0
parent2: -
mutation_kind: mutate

change_summary:

- DEPTH: 12 -> 11 (AR 42 -> 46, model_dim=512)

parent_metrics:

- val_bpb: 0.980011
- params_m: 71.3
- num_steps: 1366

child_metrics:

- val_bpb: 0.979023
- params_m: 68.2
- num_steps: 1465

deltas:

- delta_val_bpb: -0.000988 (real new best)

decision:

- status: elite, replace_slot=elite/0

judgment:

- big finding: with all the other tweaks (short=128, ve_gate=96, WD=0.15, WARMDOWN=0.8), DEPTH=11 now beats DEPTH=12. The added regularization and smaller window shifted the optimal depth slightly down.
- worth_it: yes — meaningful gain.

revisit_later:

- yes — try DEPTH=10 next, to find new floor

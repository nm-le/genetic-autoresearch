# exp0056 — ve_gate_channels 32 → 64 (isolated)

- parent1: d07e930 (elite/0 best, depth=10, lr=0.03, warmup, base=10000)
- parent2: -
- operator: mutate
- what changed: `self.ve_gate_channels = 32 → 64`. Isolated on clean
  best base.

## Parent metrics
- elite/0 (best): val_bpb=0.985189

## Child metrics
- val_bpb: 0.985257
- peak_vram_mb: 67805.3
- num_params_M: 85.9
- num_steps: 1122
- training_seconds: 300.2

## Deltas
- vs parent1: +0.000068 (within noise floor)
- vs base=10000 replicate mean (0.985321): -0.000064 (within noise)

## Decision
ve_gate_channels=64 has no detectable effect on val_bpb at noise floor.
Doesn't help, doesn't hurt. The exp0054 effect was the rotary base
regression, not the gate widening. Don't promote.

## Revisit
- Skip ve_gate_channels tuning. Default 32 is fine.
- Try ve_gate_channels=8 or 16 (simplification — fewer gate params).

## Closest prior experiment
exp0042 (best, channels=32, val=0.985189). Δ = +0.000068 (noise).

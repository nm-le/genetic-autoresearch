# exp0005 — crossover elite/0 (baseline) + elite/2 → depth=8 + WARMDOWN_RATIO=0.3

- parent1: `fcc3407` (elite/0, baseline depth=8)
- parent2: `66ca8f1` (elite/2, depth=9 + warmdown=0.3)
- operator: crossover
- what changed: from baseline (depth=8), transplanted ONE coherent idea from elite/2 — the WARMDOWN_RATIO 0.5 → 0.3 schedule change. Did NOT also pull in the depth=9 part. New combination: depth=8 + WARMDOWN_RATIO=0.3.
- parent metrics:
  - elite/0: val_bpb=0.996898, num_steps=926
  - elite/2: val_bpb=1.020408, num_steps=617
- child metrics:
  - val_bpb: 0.998669
  - peak_vram_mb: 45060.2 (~44.0 GB)
  - num_params_M: 50.3
  - num_steps: 926
  - mfu_percent: 38.68
- deltas:
  - vs baseline elite/0: **+0.001771 (slightly WORSE)**
  - vs depth=9 sibling exp0003: −0.021739 (better, of course — same warmdown but smaller model)
- decision summary: warmdown=0.3 is also slightly worse at depth=8 than baseline warmdown=0.5. Two-axis confirmation that 0.3 is not the right warmdown ratio. Promoted to elite/2 (replacing the previous depth=9+warmdown=0.3 entry that this strictly dominates), which is a frontier cleanup win even though it's not a new best.
- revisit: try warmdown sweep in the OTHER direction (0.5 → 0.6 or 0.7) to see if more warmdown helps at depth=8. Reducing warmdown made things worse → maybe extending it helps.
- closest prior: exp0001 (baseline) for arch, exp0003 for warmdown=0.3 setting.
